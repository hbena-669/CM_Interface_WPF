<UserControl x:Class="Interface_WPF.Content.Views.HomeComptaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             xmlns:local="clr-namespace:Interface_WPF.Content.Views"
             xmlns:local1="clr-namespace:Interface_WPF.Converters"
             xmlns:vm="clr-namespace:Interface_WPF.Content.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.Resources>
        <local1:CodeToImageSourceConverter x:Key="CodeToImageConverter"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Titre -->
        <TextBlock Grid.Row="0"
               Text="Sur quel établissement travailler ?"
               FontSize="18"
               FontWeight="Bold"
               Foreground="Black"
               Margin="20,20,20,16"
               HorizontalAlignment="Left"/>

        <!-- Liste des établissements -->
        <ListView Grid.Row="1"
              ItemsSource="{Binding Etablissements}"
              BorderThickness="0"
              Background="Transparent"
              Margin="20,0,20,20"
              ScrollViewer.HorizontalScrollBarVisibility="Disabled"
              ScrollViewer.VerticalScrollBarVisibility="Auto">

            <!-- Style des items -->
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0"/>
                    <Setter Property="Height" Value="156"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="VerticalContentAlignment" Value="Top"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListViewItem">
                                <ContentPresenter />
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListView.ItemContainerStyle>

            <!-- Layout en grille 3 colonnes -->
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="3" VerticalAlignment="Top" Margin="10,20,10,10"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>

            <!-- Template de chaque card -->
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Button Padding="12"
                        Margin="8"
                        Height="140"
                        HorizontalContentAlignment="Stretch"
                        VerticalContentAlignment="Stretch"
                        ToolTip="{Binding Code}"
                        cal:Message.Attach="Open($dataContext)"
                        Background="White"
                        BorderBrush="#CC6600"
                        BorderThickness="2"
                        Cursor="Hand">

                        <!-- Style avec effet hover -->
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="border"
                                                Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="8">
                                                <Border.Effect>
                                                    <DropShadowEffect BlurRadius="8" 
                                                                ShadowDepth="2" 
                                                                Opacity="0.1" 
                                                                Color="#000000"/>
                                                </Border.Effect>
                                                <ContentPresenter Margin="{TemplateBinding Padding}"
                                                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <!-- Effet hover -->
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="border" Property="BorderBrush" Value="#1565C0"/>
                                                    <Setter TargetName="border" Property="Effect">
                                                        <Setter.Value>
                                                            <DropShadowEffect BlurRadius="12" 
                                                                        ShadowDepth="3" 
                                                                        Opacity="0.2" 
                                                                        Color="#1565C0"/>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Trigger>
                                                <!-- Effet click -->
                                                <Trigger Property="IsPressed" Value="True">
                                                    <Setter TargetName="border" Property="Background" Value="#F5F5F5"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>

                        <!-- Contenu de la card -->
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <!-- Logo centré -->
                            <Image Grid.Row="0"
                               Width="60"
                               Height="60"
                               Margin="0,0,0,8"
                               HorizontalAlignment="Center"
                               Stretch="Uniform"
                               Source="{Binding Code, Converter={StaticResource CodeToImageConverter}}" />

                            <!-- Nom de l'établissement -->
                            <TextBlock Grid.Row="1"
                                   Text="{Binding Nom}"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Foreground="#1565C0"
                                   TextAlignment="Center"
                                   TextWrapping="Wrap"
                                   VerticalAlignment="Top"
                                   MaxHeight="40"
                                   TextTrimming="CharacterEllipsis"
                                   HorizontalAlignment="Center"/>
                        </Grid>
                    </Button>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>


</UserControl>
