<UserControl x:Class="Interface_WPF.Content.Views.BookView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <materialDesign:Card Width="270" Margin="10">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Titre -->
            <TextBlock Grid.Row="0"
                       Text="{Binding Name, FallbackValue='Book Name'}"
                       FontSize="18"
                       FontWeight="Bold"
                       Foreground="{DynamicResource PrimaryHueMidBrush}"
                       Margin="0 0 0 5" />

            <!-- Auteur -->
            <TextBlock Grid.Row="1"
                       Text="{Binding Author, FallbackValue='Book Author'}"
                       FontStyle="Italic"
                       TextWrapping="Wrap"
                       Margin="0 0 0 5" />

            <!-- Nombre de pages -->
            <TextBlock Grid.Row="2"
                       Text="{Binding PageCount, FallbackValue='Page Count: 0'}"
                       Foreground="Gray"
                       Margin="0 0 0 5" />

            <!-- Catégorie -->
            <ComboBox Grid.Row="3"
                      ItemsSource="{Binding Categories}"
                      SelectedItem="{Binding Category, Mode=TwoWay}"
                      Width="180"
                      IsEnabled="False"
                      Margin="0 0 0 5"
                      Style="{StaticResource MaterialDesignComboBox}" 
                      HorizontalAlignment="Left"
                      FontSize="14"
                      BorderBrush="DarkSlateBlue"
                      Foreground="DarkSlateBlue" 
                      Background="Black"/>

            <!-- Bouton Supprimer -->
            <StackPanel Grid.Row="4"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Bottom">
                <Button x:Name="Remove"
                        Style="{StaticResource MaterialDesignFlatButton}"
                        Margin="0"
                        ToolTip="Supprimer ce livre">
                    <StackPanel Orientation="Horizontal">
                        <fa:ImageAwesome Icon="Trash"
                                         Width="16"
                                         Height="16"
                                         Foreground="Red"
                                         Margin="0 0 5 0"/>
                        <TextBlock Text="Supprimer"
                                   Foreground="Red"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>
    </materialDesign:Card>
</UserControl>
