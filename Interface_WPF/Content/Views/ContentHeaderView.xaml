<UserControl x:Class="Interface_WPF.Content.Views.ContentHeaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             xmlns:local="clr-namespace:Interface_WPF.Content.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="LightBlue" >
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
            <!-- Icône utilisateur -->
            <fa:ImageAwesome Icon="UserCircle" Width="24" Height="24" Margin="0,0,5,0" />

            <!-- Nom de l'utilisateur -->
            <TextBlock Text="{Binding UserName}" FontSize="16" VerticalAlignment="Center" Margin="0,0,5,0"/>
        </StackPanel>
    </Grid>
</UserControl>
